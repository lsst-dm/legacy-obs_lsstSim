config.calibrate.photoCal.match.sourceSelection.doUnresolved = False

# Allows u-band to lock on to correct locus
# Does not seem to hurt r-band data
config.charImage.measurePsf.sourceSelector["objectSize"].widthStdAllowed = 1.
